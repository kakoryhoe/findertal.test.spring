apply plugin: "war"

bootWar {
    mainClassName = "com.blog.car.CarBlogApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
